/**
 * Enum des types de Vector Store supportés.
 */
export enum VectorStoreType {
  MEMORY = "memory",
  PINECONE = "pinecone",
  QDRANT = "qdrant",
  FAISS = "faiss"
  // Ajouter d'autres stores ici
}
